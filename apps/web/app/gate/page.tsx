import { FeaturePage } from "../app-shell";
export default function GatePage() { return <FeaturePage title="Gate simulator" description="Ticket entry and exit validation will be connected here. Gate API keys remain server-side."/>; }
