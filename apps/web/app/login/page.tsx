'use client';
import { FormEvent, useState } from 'react';
let accessTokenInMemory = '';

export default function LoginPage() { const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); const [success,setSuccess]=useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); setError(''); setSuccess(false); setLoading(true); try { const res=await fetch(`${process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3001'}/api/v1/auth/login`,{method:'POST',headers:{'content-type':'application/json'},credentials:'include',body:JSON.stringify({email,password})}); const data=await res.json(); if(!res.ok) throw new Error(data.message ?? 'Email or password is incorrect.'); accessTokenInMemory=data.accessToken; setSuccess(true); } catch(e){setError(e instanceof Error?e.message:'Service temporarily unavailable.');} finally{setLoading(false);} }
  return <main className="auth-shell"><section className="auth-visual"><div className="brand">MetroFlow</div><div className="visual-copy"><h1>Your journey starts here.</h1><p>Sign in to manage your tickets, journeys, and account securely.</p></div><div className="visual-foot">Smart transit, clear journeys.</div></section><section className="auth-panel"><div className="auth-card"><div className="eyebrow">Welcome back</div><h2>Sign in</h2><p className="subtle">Use your MetroFlow account to continue.</p><form onSubmit={submit}><label className="field">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" /></label><label className="field">Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" /></label><p className="auth-link" style={{textAlign:'right',marginTop:'-4px'}}><a href="/forgot-password">Forgot password?</a></p>{error&&<div className="feedback error" role="alert">{error}</div>}{success&&<div className="feedback success" role="status">Signed in successfully.</div>}<button className="primary" disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form><p className="auth-link">New to MetroFlow? <a href="/register">Create an account</a></p></div></section></main>;
}
