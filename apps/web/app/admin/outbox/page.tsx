"use client";
import { useEffect, useState } from "react";
import { AppShell } from "../../app-shell";
import { getAccessToken, refreshAccessToken } from "../../auth-client";
const api = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";
type Event = { id: string; event_type: string; topic: string; status: string; attempts: number; last_error?: string | null; created_at: string; published_at?: string | null };
export default function AdminOutboxPage() { const [events, setEvents] = useState<Event[]>([]); const [error, setError] = useState(""); useEffect(() => { void (async () => { const token = getAccessToken() ?? (await refreshAccessToken()); if (!token) return setError("Your operations session has expired."); const response = await fetch(`${api}/api/v1/admin/outbox`, { credentials: "include", headers: { authorization: `Bearer ${token}` } }); const body = await response.json(); if (!response.ok) return setError(body.message ?? "Unable to load outbox events."); setEvents(body.data.events); })().catch(() => setError("Unable to load outbox events.")); }, []); return <AppShell eyebrow="Administration"><div className="admin-header"><div><span className="sim-kicker">EVENT DELIVERY</span><h1 className="page-title">Outbox</h1><p>Monitor domain events waiting to be published or requiring attention.</p></div></div>{error ? <p className="admin-notice error">{error}</p> : <div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Created</th><th>Event</th><th>Topic</th><th>Status</th><th>Attempts</th><th>Error</th></tr></thead><tbody>{events.map((event) => <tr key={event.id}><td>{new Date(event.created_at).toLocaleString()}</td><td>{event.event_type}</td><td>{event.topic}</td><td><span className="admin-tag">{event.status}</span></td><td>{event.attempts}</td><td>{event.last_error ?? "—"}</td></tr>)}</tbody></table></div>}</AppShell>; }
