"use client";
import { useEffect, useState } from "react";
import { AppShell } from "../../app-shell";
import { getAccessToken, refreshAccessToken } from "../../auth-client";
const api = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";
type Ticket = { id: string; email: string; originStation: { name: string }; destinationStation: { name: string }; paidAmount: string; currency: string; status: string; expiresAt: string };
export default function AdminTicketsPage() {
  const [tickets, setTickets] = useState<Ticket[]>([]); const [page, setPage] = useState(1); const [totalPages, setTotalPages] = useState(1); const [status, setStatus] = useState(""); const [error, setError] = useState("");
  useEffect(() => { void (async () => { const token = getAccessToken() ?? (await refreshAccessToken()); if (!token) return setError("Your operations session has expired."); const query = new URLSearchParams({ page: String(page), pageSize: "25" }); if (status) query.set("status", status); const response = await fetch(`${api}/api/v1/tickets/operations?${query}`, { credentials: "include", headers: { authorization: `Bearer ${token}` } }); const body = await response.json(); if (!response.ok) return setError(body.message ?? "Unable to load tickets."); setTickets(body.data.tickets); setTotalPages(body.data.pagination.totalPages || 1); })().catch(() => setError("Unable to load tickets.")); }, [page, status]);
  return <AppShell eyebrow="Administration"><div className="admin-header"><div><span className="sim-kicker">OPERATIONS</span><h1 className="page-title">Ticket operations</h1><p>Inspect passenger tickets with predictable, paginated loading.</p></div></div><label style={{display:"inline-flex",flexDirection:"column",gap:6,marginBottom:16}}>Status<select aria-label="Filter ticket status" value={status} onChange={(event) => { setStatus(event.target.value); setPage(1); }}><option value="">All statuses</option><option value="ISSUED">Issued</option><option value="IN_JOURNEY">In journey</option><option value="COMPLETED">Completed</option><option value="EXPIRED">Expired</option></select></label>{error ? <p className="admin-notice error">{error}</p> : <><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Passenger</th><th>Route</th><th>Fare</th><th>Status</th><th>Expires</th></tr></thead><tbody>{tickets.map((ticket) => <tr key={ticket.id}><td>{ticket.email}</td><td><strong>{ticket.originStation.name} to {ticket.destinationStation.name}</strong></td><td>{ticket.currency} {ticket.paidAmount}</td><td><span className="admin-tag">{ticket.status}</span></td><td>{new Date(ticket.expiresAt).toLocaleString()}</td></tr>)}</tbody></table></div><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginTop:16}}><span>Page {page} of {totalPages}</span><div style={{display:"flex",gap:8}}><button className="button secondary" disabled={page <= 1} onClick={() => setPage((value) => value - 1)}>Previous</button><button className="button" disabled={page >= totalPages} onClick={() => setPage((value) => value + 1)}>Next</button></div></div></>}</AppShell>;
}
