"use client";
import { useEffect, useState } from "react";
import { AppShell } from "../../app-shell";
import { getAccessToken, refreshAccessToken } from "../../auth-client";
const api = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";
type GateEvent = { id: string; event_type: string; rejection_reason?: string | null; request_id?: string | null; occurred_at: string; gate_code: string; station_name: string; ticket_id?: string | null };
export default function GateEventsPage() { const [events, setEvents] = useState<GateEvent[]>([]); const [error, setError] = useState(""); useEffect(() => { void (async () => { const token = getAccessToken() ?? (await refreshAccessToken()); if (!token) return setError("Your operations session has expired."); const response = await fetch(`${api}/api/v1/gate/operations/events`, { credentials: "include", headers: { authorization: `Bearer ${token}` } }); const body = await response.json(); if (!response.ok) return setError(body.message ?? "Unable to load gate events."); setEvents(body.data.events); })().catch(() => setError("Unable to load gate events.")); }, []); return <AppShell eyebrow="Administration"><div className="admin-header"><div><span className="sim-kicker">AUDIT LOG</span><h1 className="page-title">Gate events</h1><p>Review every accepted and rejected entry or exit scan.</p></div></div>{error ? <p className="admin-notice error">{error}</p> : <div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Time</th><th>Station / gate</th><th>Event</th><th>Reason</th><th>Ticket</th><th>Request ID</th></tr></thead><tbody>{events.map((event) => <tr key={event.id}><td>{new Date(event.occurred_at).toLocaleString()}</td><td>{event.station_name} · {event.gate_code}</td><td><span className="admin-tag">{event.event_type}</span></td><td>{event.rejection_reason ?? "—"}</td><td>{event.ticket_id ?? "—"}</td><td className="admin-mono">{event.request_id ?? "—"}</td></tr>)}</tbody></table></div>}</AppShell>; }
