"use client";
import { useEffect, useState } from "react";
import { AppShell } from "../../app-shell";
import { getAccessToken, refreshAccessToken } from "../../auth-client";
const api = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";
type Journey = { id: string; email: string; status: string; entryStation: { name: string }; actualExitStation?: { name: string } | null; enteredAt: string; exitedAt?: string | null; expiresAt: string };
export default function AdminJourneysPage() { const [items, setItems] = useState<Journey[]>([]); const [page, setPage] = useState(1); const [pages, setPages] = useState(1); const [status, setStatus] = useState(""); const [error, setError] = useState(""); useEffect(() => { void (async () => { const token = getAccessToken() ?? (await refreshAccessToken()); if (!token) return setError("Your operations session has expired."); const query = new URLSearchParams({page:String(page),pageSize:"25"}); if (status) query.set("status",status); const response = await fetch(`${api}/api/v1/journeys/operations?${query}`, {credentials:"include",headers:{authorization:`Bearer ${token}`}}); const body = await response.json(); if (!response.ok) return setError(body.message ?? "Unable to load journeys."); setItems(body.data.journeys); setPages(body.data.pagination.totalPages || 1); })().catch(() => setError("Unable to load journeys.")); }, [page,status]); return <AppShell eyebrow="Administration"><div className="admin-header"><span className="sim-kicker">OPERATIONS</span><h1 className="page-title">Journey operations</h1><p>Monitor passenger journeys by lifecycle status.</p></div><label style={{display:"inline-flex",flexDirection:"column",gap:6,marginBottom:16}}>Status<select aria-label="Filter journey status" value={status} onChange={e=>{setStatus(e.target.value);setPage(1);}}><option value="">All statuses</option><option value="ACTIVE">Active</option><option value="COMPLETED">Completed</option><option value="TIMED_OUT">Timed out</option></select></label>{error?<p className="admin-notice error">{error}</p>:<><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Passenger</th><th>Entry</th><th>Exit</th><th>Status</th><th>Entered</th><th>Exited / expires</th></tr></thead><tbody>{items.map(j=><tr key={j.id}><td>{j.email}</td><td>{j.entryStation.name}</td><td>{j.actualExitStation?.name ?? "—"}</td><td><span className="admin-tag">{j.status}</span></td><td>{new Date(j.enteredAt).toLocaleString()}</td><td>{j.exitedAt?new Date(j.exitedAt).toLocaleString():new Date(j.expiresAt).toLocaleString()}</td></tr>)}</tbody></table></div><div style={{display:"flex",justifyContent:"space-between",marginTop:16}}><span>Page {page} of {pages}</span><div><button className="button secondary" disabled={page<=1} onClick={()=>setPage(v=>v-1)}>Previous</button> <button className="button" disabled={page>=pages} onClick={()=>setPage(v=>v+1)}>Next</button></div></div></>}</AppShell>; }
